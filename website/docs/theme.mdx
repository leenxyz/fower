---
id: theme
title: Theme
---

## Installation

```bash
npm i @styli/theming
```

## Usage

### Use ThemeProvider

At the top level of React App, use ThemeProvider to inject the theme object.

```jsx title="App.tsx"
import React from 'react'
import { ThemeProvider } from '@styli/theming'

const theme = {
  colors: {
    primary: 'deeppink',
  },
}

export const App = ({ children }) => {
  return (
    <div>
      <ThemeProvider theme={theme}>{children}</ThemeProvider>
    </div>
  )
}
```

### Use in Component

After injecting the theme to the ThemeProvider, you can use the theme in any Atomic Props.

```jsx title="pages/Home.tsx"
import React from 'react'
import { Text } from '@styli/react'

export const Home = ({ children }) => {
  return (
    <div>
      <Text color={(theme) => theme.colors.primary}></Text>
    </div>
  )
}
```

### useTheme

你也可以使用 Styli 提供的一个 React Hooks: `useTheme`，更灵活的使用主题。

```jsx title="pages/Home.tsx"
import React from 'react'
import { Text } from '@styli/react'
import { useTheme } from '@styli/theming'

export const Home = ({ children }) => {
  const theme = useTheme()
  return (
    <div>
      <Text color={theme.colors.primary}></Text>
    </div>
  )
}
```
